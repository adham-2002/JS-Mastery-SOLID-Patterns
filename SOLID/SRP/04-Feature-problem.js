//h2 🎯 Scenario:
/**
 * h1 Assume your company decides to add a "Cancel Order & Refund Features 🛑 "
 * * we need to reverse the payment process and update the system accordingly
 * * 1) payment logic we need to handle both payments and refunds
 * * 2) we have to modify email notification to send different message for refunds
 * * 3) if we change payment(switch from credit card to paypal) we have to update multiple parts of the code instead of just one
 */
